<Project>
  <PropertyGroup>
    <VersionPrefix>0.1.0</VersionPrefix>
    <VersionSuffix Condition="'$(VersionSuffix)' == '' AND '$(BUILD_NUMBER)' != ''">$(BUILD_NUMBER)</VersionSuffix>
  </PropertyGroup>
</Project>
