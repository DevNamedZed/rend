namespace Rend
{
    /// <summary>
    /// Output format for raster image rendering.
    /// </summary>
    public enum ImageOutputFormat
    {
        /// <summary>PNG format (lossless).</summary>
        Png,
        /// <summary>JPEG format (lossy).</summary>
        Jpeg,
        /// <summary>WebP format.</summary>
        WebP
    }
}
