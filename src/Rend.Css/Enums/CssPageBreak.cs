namespace Rend.Css
{
    /// <summary>CSS page-break property values.</summary>
    public enum CssPageBreak : byte
    {
        Auto,
        Always,
        Avoid,
        Left,
        Right
    }
}
