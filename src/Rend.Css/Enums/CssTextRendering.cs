namespace Rend.Css
{
    /// <summary>CSS text-rendering property values.</summary>
    public enum CssTextRendering : byte
    {
        Auto,
        OptimizeSpeed,
        OptimizeLegibility,
        GeometricPrecision
    }
}
