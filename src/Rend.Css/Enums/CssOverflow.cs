namespace Rend.Css
{
    /// <summary>CSS overflow property values.</summary>
    public enum CssOverflow : byte
    {
        Visible,
        Hidden,
        Scroll,
        Auto,
        Clip
    }
}
