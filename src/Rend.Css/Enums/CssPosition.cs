namespace Rend.Css
{
    /// <summary>CSS position property values.</summary>
    public enum CssPosition : byte
    {
        Static,
        Relative,
        Absolute,
        Fixed,
        Sticky
    }
}
