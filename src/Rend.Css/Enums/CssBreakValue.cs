namespace Rend.Css
{
    /// <summary>CSS break-before/break-after/break-inside property values.</summary>
    public enum CssBreakValue : byte
    {
        Auto,
        Avoid,
        Always,
        Page,
        Left,
        Right,
        Column,
        AvoidPage,
        AvoidColumn
    }
}
