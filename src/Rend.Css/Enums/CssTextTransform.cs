namespace Rend.Css
{
    /// <summary>CSS text-transform property values.</summary>
    public enum CssTextTransform : byte
    {
        None,
        Uppercase,
        Lowercase,
        Capitalize
    }
}
