namespace Rend.Css
{
    /// <summary>CSS grid-auto-flow property values.</summary>
    public enum CssGridAutoFlow : byte
    {
        Row,
        Column,
        RowDense,
        ColumnDense
    }
}
