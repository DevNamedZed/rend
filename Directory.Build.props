<Project>
  <Import Project="Version.props" />

  <PropertyGroup>
    <DefaultItemExcludes>$(DefaultItemExcludes);conformance\Rend.VisualRegression\output\**;conformance\Rend.VisualRegression\results\history\**</DefaultItemExcludes>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>CS1591</NoWarn>
  </PropertyGroup>

  <PropertyGroup>
    <UseSharedCompilation>false</UseSharedCompilation>
    <RunAnalyzersDuringBuild>false</RunAnalyzersDuringBuild>
  </PropertyGroup>

  <!-- Deterministic and reproducible builds -->
  <PropertyGroup>
    <Deterministic>true</Deterministic>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
  </PropertyGroup>

  <!-- NuGet package metadata -->
  <PropertyGroup>
    <Authors>DevNamedZed</Authors>
    <Company>Rend</Company>
    <Copyright>Copyright (c) 2026 DevNamedZed</Copyright>
    <Description>HTML/CSS rendering engine for .NET. Renders to PDF and images.</Description>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/DevNamedZed/rend</PackageProjectUrl>
    <RepositoryUrl>https://github.com/DevNamedZed/rend</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>html;css;pdf;render;image;layout</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <!-- SourceLink for NuGet debugging -->
  <ItemGroup>
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="8.0.0" PrivateAssets="All" />
  </ItemGroup>

  <!-- Include README in package -->
  <ItemGroup>
    <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
